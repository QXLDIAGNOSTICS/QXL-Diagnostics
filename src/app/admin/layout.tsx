import type { ReactNode } from "react";

/** Thin layout — pages redirect to admin.qxldiagnostics.com */
export default function LegacyAdminLayout({ children }: { children: ReactNode }) {
  return children;
}
