import PortalPage from "@/components/PortalPage";

export default function CollectorLoginPage() {
  return <PortalPage portalName="Phlebotomist Login Portal" portalRole="Phlebotomist Login" description="Sign in to view daily pickup routes." activeTabKey="login" />;
}
